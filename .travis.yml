language: node_js
node_js:
  - "8"
install: yarn
jobs:
  include:
    - stage: test
      script: yarn run lint
    - stage: deploy
      script: bash ./scripts/.travis-deploy.sh
cache:
  yarn: true
  directories:
    - node_modules
